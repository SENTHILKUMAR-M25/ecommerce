<div
  style={{
    maxWidth: "64rem",
    margin: "0 auto",
    background: "#fff",
    borderRadius: "24px",
    boxShadow: "0 10px 30px rgba(0,0,0,0.1)",
    border: "1px solid #e2e8f0",
    overflow: "hidden",
  }}
>
  {/* Header */}
  <div
    style={{
      padding: "48px",
      borderBottom: "1px solid #e2e8f0",
      background: "linear-gradient(to bottom, #f8fafc, transparent)",
      display: "flex",
      justifyContent: "space-between",
      gap: "24px",
      flexWrap: "wrap",
    }}
  >
    <div>
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "8px",
          color: "#06b6d4",
          fontWeight: "700",
          textTransform: "uppercase",
          fontSize: "12px",
          marginBottom: "8px",
        }}
      >
        <ShoppingBag style={{ width: 16, height: 16 }} />
        <span>${order.orderStatus}</span>
      </div>

      <h1
        style={{
          fontSize: "48px",
          fontWeight: 900,
          letterSpacing: "-1px",
          color: "#0f172a",
        }}
      >
        INVOICE
      </h1>

      <p style={{ fontSize: "13px", color: "#64748b", marginTop: "8px" }}>
        Invoice ID:${" "}
        <span style={{ fontFamily: "monospace", color: "#334155" }}>
          #${order._id}
        </span>
      </p>
    </div>

    <div style={{ textAlign: "right" }}>
      <h2
        style={{
          fontSize: "28px",
          fontWeight: 900,
          background: "linear-gradient(to right, #06b6d4, #2563eb)",
          WebkitBackgroundClip: "text",
          WebkitTextFillColor: "transparent",
        }}
      >
        VS STORE
      </h2>

      <p style={{ fontSize: "13px", marginTop: "6px", color: "#475569" }}>
        Madurai, Tamil Nadu
      </p>

      <p style={{ fontSize: "11px", marginTop: "6px", color: "#94a3b8" }}>
        Date:{" "}
        ${new Date(order.createdAt).toLocaleDateString(undefined, {
          dateStyle: "long",
        })}
      </p>
    </div>
  </div>

  {/* Client + Shipping */}
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: "32px",
      padding: "48px",
      background: "#f8fafc",
      borderBottom: "1px solid #e2e8f0",
    }}
  >
    <div>
      <h3
        style={{
          fontSize: "11px",
          textTransform: "uppercase",
          color: "#94a3b8",
          marginBottom: "12px",
          fontWeight: 700,
        }}
      >
        Client Details
      </h3>

      <p style={{ fontWeight: 700, fontSize: "16px", color: "#0f172a" }}>
        ${order.user?.name}
      </p>

      <p style={{ fontSize: "13px", color: "#64748b" }}>
        ${order.user?.email}
      </p>

      <p style={{ marginTop: "10px", fontSize: "12px" }}>
        Payment:{" "}
        <span
          style={{
            background: "#e2e8f0",
            padding: "4px 8px",
            borderRadius: "6px",
            marginLeft: "4px",
            fontWeight: 500,
          }}
        >
          {order.paymentMethod}
        </span>
      </p>
    </div>

    <div>
      <h3
        style={{
          fontSize: "11px",
          textTransform: "uppercase",
          color: "#94a3b8",
          marginBottom: "12px",
          fontWeight: 700,
        }}
      >
        Shipping Address
      </h3>

      <p style={{ fontWeight: 600, color: "#1e293b" }}>
        ${order.shippingAddress.street}
      </p>

      <p style={{ color: "#475569" }}>
        ${order.shippingAddress.city}, {order.shippingAddress.state}
      </p>

      <p style={{ fontSize: "12px", color: "#64748b" }}>
        ${order.shippingAddress.zipCode}
      </p>

      <p style={{ fontSize: "12px", color: "#94a3b8" }}>
        ${order.shippingAddress.country}
      </p>
    </div>
  </div>

  {/* Table */}
  <div style={{ padding: "32px", overflowX: "auto" }}>
    <table style={{ width: "100%", minWidth: "600px", borderCollapse: "collapse" }}>
      <thead>
        <tr style={{ borderBottom: "1px solid #e2e8f0", fontSize: "12px" }}>
          <th style={{ textAlign: "left", paddingBottom: "12px" }}>Product</th>
          <th style={{ textAlign: "center", paddingBottom: "12px" }}>Qty</th>
          <th style={{ textAlign: "right", paddingBottom: "12px" }}>Price</th>
          <th style={{ textAlign: "right", paddingBottom: "12px" }}>Total</th>
        </tr>
      </thead>

      <tbody>
        ${order.orderItems.map((item, index) => (
          <tr key=${index} style={{ borderBottom: "1px solid #f1f5f9" }}>
            <td style={{ padding: "16px" }}>
              <div style={{ display: "flex", gap: "12px", alignItems: "center" }}>
                <img
                  src=${item.image}
                  style={{
                    width: "48px",
                    height: "48px",
                    borderRadius: "12px",
                    objectFit: "cover",
                  }}
                />
                <div>
                  <p style={{ fontWeight: 600 }}>${item.name}</p>
                  {item.variant && (
                    <p style={{ fontSize: "11px", color: "#94a3b8" }}>
                      ${item.variant}
                    </p>
                  )}
                </div>
              </div>
            </td>

            <td style={{ textAlign: "center", fontFamily: "monospace" }}>
              ${item.quantity}
            </td>

            <td style={{ textAlign: "right" }}>₹${item.price.toFixed(2)}</td>

            <td style={{ textAlign: "right", fontWeight: 700 }}>
              ₹${(item.quantity * item.price).toFixed(2)}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>

  {/* Totals */}
  <div style={{ padding: "48px", display: "flex", justifyContent: "flex-end" }}>
    <div style={{ width: "320px", fontSize: "14px" }}>
      <div style={{ display: "flex", justifyContent: "space-between" }}>
        <span>Items Total</span>
        <span>₹${order.itemsPrice.toFixed(2)}</span>
      </div>

      <div style={{ display: "flex", justifyContent: "space-between" }}>
        <span>Shipping</span>
        <span>₹${order.shippingPrice.toFixed(2)}</span>
      </div>

      <div style={{ display: "flex", justifyContent: "space-between" }}>
        <span>Tax</span>
        <span>₹${order.taxPrice.toFixed(2)}</span>
      </div>

      <div
        style={{
          display: "flex",
          justifyContent: "space-between",
          color: "green",
        }}
      >
        <span>Discount</span>
        <span>- ₹${order.discountPrice.toFixed(2)}</span>
      </div>

      <hr style={{ margin: "12px 0" }} />

      <div
        style={{
          display: "flex",
          justifyContent: "space-between",
          fontSize: "22px",
          fontWeight: 900,
          color: "#06b6d4",
        }}
      >
        <span>Total</span>
        <span>₹${order.totalPrice.toFixed(2)}</span>
      </div>
    </div>
  </div>

  {/* Footer */}
  <div
    style={{
      textAlign: "center",
      padding: "32px",
      borderTop: "1px solid #e2e8f0",
      background: "#f8fafc",
    }}
  >
    <h3 style={{ fontWeight: 700 }}>Thank You For Your Purchase ❤️</h3>
    <p style={{ fontSize: "12px", color: "#94a3b8" }}>
      This is an electronically generated document. No signature required.
    </p>
  </div>
</div>